# Tasks: r0-kernel-foundation

## 1. ADR and dependency admission (before any code)

- [x] 1.1 Write `docs/adr/ADR-0005-storage-engine-abstraction.md`: PostgreSQL wire protocol as primary persistence surface, postgres.js as driver, no platform SDK in core, Supabase-optional stance, SQLite not admitted (in-memory adapter covers tests). Status: Accepted. Verification: file exists, ADR index/docs updated if the repo tracks one.
- [x] 1.2 Add `postgres` (postgres.js, exact pin) to root allowlist/toolchain notes per standards 09; record zod 4.4.3 reuse decision in ADR-0005. Verification: `pnpm install` clean, no version drift.

## 2. Workspace units (ADR-0002: unit exists only with manifest + admitted code)

- [x] 2.1 Create `packages/domain`: package.json (name `@navis/domain`, private, type module, exports `.` only (single public surface; schema/state/ports stay internal until a concrete external consumer exists — standards 01: broad barrels are not default architecture), deps: zod 4.4.3), tsconfig.json + tsconfig.test.json extending root base, no other Navis dependency. Verification: `pnpm install` links it, `pnpm --filter @navis/domain typecheck` passes.
- [x] 2.2 Create `packages/infrastructure`: package.json (name `@navis/infrastructure`, deps: `@navis/domain`, `postgres`), tsconfig pair. Verification: same as 2.1.
- [x] 2.3 Update root `dependency-cruiser.config.cjs` to include both new units with ADR-0001 rules (domain imports no Navis package; infrastructure may import domain; neither may import contracts transport). Verification: `pnpm boundaries` passes with the new packages present.

## 3. Domain object model (`packages/domain/src/schema`)

- [x] 3.1 Implement `participant.ts` (Participant registry type: id, project_id, type human/agent, display_name?, role? — the system's only actor identity), `ids.ts` (UUIDv7 time-ordered branded id schemas; generator implemented in-repo per RFC 9562 §5.1 with unix_ts_ms_v7 — zero dependencies, Node crypto only for randomness), `text.ts`/`time.ts` (shared constraints each used across 6+ model files); ref fields use `uuidSchema` directly and title/version constraints inline at their ≤3 use sites — model files import no other model's symbols (standards 01 self-containment). Verification: unit tests — UUIDv7 lexicographic ordering matches creation time (spec scenario), monotonic counter within the same millisecond, each schema accepts/rejects.
- [x] 3.2 Implement the object schemas per the accepted baseline: `project.ts`, `work.ts`, `taskspace.ts`, `asset.ts` (kind 7 values, scope 5 values, project_id required unless scope=organization, content carrier object), `acceptance.ts` (target_ref/target_type Asset-only; criteria_snapshot required; evidence_refs ≤100; rationale required iff result ∈ {rejected, conditional}), `delivery.ts` (target_ref, dispatched_at, version, attempt_no, confirmation flow), `workrun.ts` (9-state status, parent_run_id, checkpoint_id, input_state_version, attempt, execution_refs JSONB, intervention sessions with consent_status), `hold.ts` (statement required, applicability renamed from validity, kind 6 values, severity 5 values, fowler_quadrant tech_debt-only, source_event_ids, asset_refs), plus `participant.ts` as the actor registry and `equip.ts`/`checkpoint.ts` as one-file-per-system-type (checkpoint: captured_at, state_version, position, resume_ref). Baseline guard test: field-set and enum-set equality against the accepted baseline (fails on any local invention — extra field, missing field, missing/extra enum value). Field-conventions guard test: assert no deleted boolean, no untyped ext passthrough, and no command-path setter for updated_at/updated_by on any schema surface. Verification: vitest — one valid construction per type + invalid-field rejection with field name + baseline guard + conventions guard.
- [x] 3.3 Implement the Asset lifecycle table inside `asset.ts` (Asset's own behavior and purge constant stay in its model file) + `src/errors/schema.ts` (schema-module error registry; `schema/` holds table structures only) (schema/ error registry: illegal-transition, not-enabled, purge-conditions-unmet; tuning-constant registry: PURGE_AGE_THRESHOLD_DAYS/COMPETITIVE_GRACE_PERIOD_DAYS — design D8): 7-state enum (contested reserved in type, unreachable), legal transition table exactly as spec lists, `assertTransition(from, to, {daysArchived, doubleConfirmation})` with double-condition purge gate, all rejections emitting registry tokens. Verification: vitest covering every legal pair (accept), every illegal pair (reject), terminal restart (reject), purge 2×2 condition matrix, contested unreachable. (Ports the verified lifecycle experiments; the fix-and-resubmit loop is implemented as a new candidate referencing the rejected predecessor — rejected stays terminal, per the corrected domain spec.)

## 4. Project-state kernel (`packages/domain/src/state`)

- [x] 4.1 Implement `events.ts`: event envelope (seq/type/data/actor/at/state_version), deep-freeze on append, canonical JSON helper. Verification: vitest — mutation attempts throw/detect, seq monotonicity.
- [x] 4.2 Implement `project-state-kernel.ts`: closed error-code registry first (`errors.ts`: typed const map of stable string codes — forbidden/rationale_required/version_conflict/equip_budget_exceeded/unaccepted_artifact/blocking_hold/unknown_effect_unclosed/project_not_active/boundary_violation/…; suite assertions bind to the registry), then append-only history (no update/delete API), replayable projection, optimistic concurrency (expected-version guard), version semantics (event seq monotonic; project_state_version advanced only by State-material events — update_boundary and project status transitions; redirect_work advances seq + Work aggregate revision without bumping project_state_version), boundary update (human-only + reason; updates Project.boundary + project_state_version), asset candidate/accept/reject/conditional flow (updates projection; Acceptance rows carry criteria_snapshot), hold register/confirm/resolve/accept/dormant/invalidate transitions per the accepted baseline lifecycle (agent-registered starts as registered and blocks nothing until human confirms to active; actor on every hold event), effect ledger (unknown→confirmed/failed; unclosed unknown blocks deliver_result), project/work lifecycle actions (create/pause/resume/complete/archive/redirect — human-gated with reason, hold closure with cause via invalidated, completion refused while blocking holds active), equip issuance (verified_facts = lifecycle active + scope project, per the accepted derivation; budget gate with diagnostics event), return (version guard, wholesale rejection, candidates+effects absorption on success), delivery gate per-asset (not-active→unaccepted→blocking_hold→unknown_effect order, one open attempt per asset+target via attempt_no, delivered event with delivering Participant). Verification: vitest suites porting the verified product assertions: replay identity on 200 events, stale-return zero-pollution, agent-hold not blocking until confirm, equip budget exceed with diagnostics, delivery gate order matrix, actor fields present on all hold events. Implement `src/errors/kernel.ts` (kernel-module error registry) and the competitive grace constant in the kernel file that consumes it. Also implement the project-time projection (updated_at/updated_by maintained EXCLUSIVELY by the projection replay path; command paths never write them — guard-tested), tombstone read-side semantics (every projection lookup and gate evaluation excludes tombstoned rows — the storage gate indexes already carry `deleted_at IS NULL`; the replay path that sets `deleted_at` from retirement events must keep those indexes correct) and the event-envelope schema_version.
- [x] 4.3 Implement `ports/event-store.ts` + `ports/clock.ts` in domain: EventStore interface (append with expected version → conflict error, loadEvents(fromSeq), saveSnapshot, loadSnapshot), zero driver types. Verification: typecheck + a compile-time `satisfies` test; grep-style boundary test that no driver import exists in `src/ports`.

## 5. Infrastructure adapters (`packages/infrastructure/src/persistence`)

- [x] 5.1 Implement `in-memory/in-memory-event-store.ts` satisfying EventStore. Verification: shared port conformance suite (same tests run against both adapters).
- [x] 5.2 Implement `postgres/postgres-event-store.ts` + `postgres/migrations/001_events.sql` (idempotent, plain SQL): L1 `project_events` (event_id uuid PK, project_id, seq, aggregate_type/aggregate_id/aggregate_revision, event_type, event_schema_version, occurred_at, recorded_at, actor_participant_id, causation_id, correlation_id, idempotency_key, payload JSONB, metadata JSONB, privacy_class, state_version; UNIQUE(project_id, seq) as the optimistic-concurrency carrier), INSERT-only trigger rejecting UPDATE/DELETE; L3 projection tables (projects/works/assets/holds/work_runs/participants/checkpoints) + `project_snapshots` (updated_at/updated_by columns replay-writable only); L2 `acceptances`/`deliveries` + `delivery_attempts` (UNIQUE(delivery_id, attempt_no)) + `effect_ledger` (status intent/sent/confirmed/failed/unknown); L4 `event_outbox`; L5 relation tables (`work_dependencies`, `hold_source_events`, `hold_assets`, `workrun_effect_refs`, `workrun_evidence_refs`, `workrun_candidate_refs`, `intervention_sessions` with consent_status); `command_inbox` (UNIQUE(project_id, idempotency_key)) + `event_retention_marks` (retention classes as projection marks; no physical deletes); partial indexes: holds (project_id) WHERE status='active' AND blocks_delivery; effect_ledger (project_id) WHERE status='unknown'; no tables for TaskSpace/Equip/Candidate + migration runner. Verification: integration vitest suite against `DATABASE_URL` when set, auto-skip when absent (spec scenario); concurrency race test (two appends same expected version → one conflict); immutability trigger test (UPDATE/DELETE rejected); gate-query uses both partial indexes; idempotent replay of same command key returns first result.
- [x] 5.3 Export a connection factory reading `DATABASE_URL` (engine-neutral: works for Supabase connection strings and self-hosted identically) with production-baseline pool tuning exposed as named constants (design D8 constants registry): max 50, idle timeout 20s, max lifetime 5min, passed as postgres.js options. Verification: unit test for config parsing incl. pool options; integration smoke when DATABASE_URL present.

## 6. Repository validation (gate before reporting done)

- [x] 6.1 Full `pnpm validate` green: format:check, lint (0 warnings), typecheck, boundaries, test (with coverage), build, openspec:validate --strict.
- [x] 6.2 Spec coverage check: every Requirement/Scenario in the three delta specs maps to at least one named vitest test; list any gap and close it. DONE — every Requirement/Scenario maps to at least one named vitest test (domain-object-model: field baseline/lifecycle/rationale tables; persistence-ports: scenarios with named tests or compile-time/storage-layer evidence; project-state-kernel scenarios bound to the kernel runtime, explicitly listed as remaining).
- [x] 6.3 Update `docs/architecture.md` logical-ownership table rows for domain/infrastructure from "Not implemented" to implemented-by-this-change; note ADR-0005 in the area table.
