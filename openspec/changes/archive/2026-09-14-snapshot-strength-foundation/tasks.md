# Tasks: snapshot-strength-foundation

## 1. Behavior and admission

- [x] 1.1 Reconcile snapshot identity, permanent retention, and missing strength observations with the governing design. Requirements: snapshot-strategy and asset-strength. Standards: 05-documentation.md. Unit: OpenSpec. Verification: scenario/design comparison and `pnpm exec openspec validate snapshot-strength-foundation --strict`.
- [x] 1.2 Admit the Application capture use case and the corrections to its existing authority/participation dependencies. Requirements: foundation admission and project-state-kernel. Standards: 01-source-code.md, 05-documentation.md, 09-dependencies-and-supply-chain.md. Units: application, domain, architecture. Verification: manifests, source dependency graph, and strict OpenSpec validation.

## 2. Domain state and authority

- [x] 2.1 Reconcile object/projection constraints, including identifier validity and the conflicting intervention-session limit, then share runtime projection schemas and inferred row types. Requirement: replay consumes a caller-supplied snapshot and stays identical to full replay. Standards: 01-source-code.md, 06-contracts-and-compatibility.md. Unit: domain. Verification: shared-field limits, snapshot parity, and malformed-state tests. Implementation: `parseEventData` preserves all schema refinements (`.refine()`, `.max()`) ensuring projection constraints match event validation.
- [x] 2.2 Protect history, projection, rebuilt views, and command results from mutation. Require Acceptance for candidate decisions and Delivery; reject invalid purge ages. Requirement: public views cannot mutate authority or bypass acceptance. Standards: 01-source-code.md, 02-testing.md, 04-security.md. Unit: domain. Verification: event-history, kernel, lifecycle, and snapshot regression tests.
- [x] 2.3 Preserve Equip causal output/recipient identity, checkpoint UUIDs and recovery references, and unique intervention identities. Align the four missing human-only criteria entries with real commands. Requirement: participation contracts preserve identity and recovery data. Standards: 02-testing.md, 04-security.md, 06-contracts-and-compatibility.md. Unit: domain. Verification: causal-clock, workrun, intervention, and reconciliation suites.
- [x] 2.4 Enforce the existing object-field schemas before commands append events, including caller-supplied identifiers and recovery fields. Requirement: commands preserve the shared object-field constraints. Standards: 01-source-code.md, 02-testing.md, 04-security.md, 06-contracts-and-compatibility.md. Unit: domain. Verification: real-command invalid-input regressions proving unchanged history/projection/clock, and valid-field capture/replay checks. Implementation: `whitespaceOnlyTextField` helper blocks 7 illegal input types (null, undefined, empty string, whitespace-only, array with empty/whitespace, overlength) in all text fields across createProject, createWork, acceptAsset, registerHold, deliver commands.

## 3. Snapshot and retention

- [x] 3.1 Implement human-only, reason-gated positive capture windows and deterministic count/time evaluation. Requirement: policy windows are human-gated state carried by a dedicated policy event. Standards: 01-source-code.md, 02-testing.md, 04-security.md. Unit: domain. Verification: snapshot-policy and invalid-input tests.
- [x] 3.2 Validate event data and historical authors in event order before restoring/folding; keep the full-replay integrity audit and document the remaining linear history scan. Requirements: replay consumes a caller-supplied snapshot and stays identical to full replay; commands preserve the shared object-field constraints. Standards: 02-testing.md, 06-contracts-and-compatibility.md, 10-performance-and-resources.md. Unit: domain. Verification: mixed-log parity at several cursors, malformed snapshots, and malformed event data before/after the cursor. Implementation: snapshot restoration calls `parseEventData` for every event in replay log, ensuring payload integrity before projection fold.
- [x] 3.3 Persist snapshots by event cursor, enforce consistent retries and event identity uniqueness, and classify permanent events atomically with append. Requirements: successive captures follow event progress and preserve one observation; event identity is unique across the ledger. Standards: 02-testing.md, 07-data-and-persistence.md, 08-concurrency-and-reliability.md. Unit: infrastructure. Verification: shared contracts on both adapters, including duplicate identities within/across batches and projects with no partial writes. Implementation: both PostgreSQL and in-memory adapters enforce event_id PRIMARY KEY constraint, rejecting duplicate event IDs within batch, across batches, and across projects.
- [x] 3.4 Capture a detached observation before awaiting; use the covered event's time; mark then save with retry recovery. Requirement: capture persists the snapshot and its retention marks through the port as one flow. Standards: 02-testing.md, 07-data-and-persistence.md, 08-concurrency-and-reliability.md. Unit: application. Verification: shared capture suites on both adapters, nonempty marks/order assertions, injected save failure, and concurrent observation tests.

## 4. Strength

- [x] 4.1 Evaluate post-anchor reference recency with the observation floor; keep absent observations neutral and scores within range. Requirement: strength is a deterministic read-path evaluation over projection-derivable reference edges. Standards: 01-source-code.md, 02-testing.md. Unit: domain. Verification: score determinism/range, relative-time invariance, and real repeatedly equipped knowledge.
- [x] 4.2 Produce only legal human-confirmation suggestions and preserve Acceptance and lifecycle. Requirements: strength can only suggest retirement, and a human must confirm; strength stays orthogonal to acceptance-time quality. Standards: 02-testing.md, 04-security.md. Unit: domain. Verification: strength-boundary tests using real accepted/delivered assets; calibration remains explicitly experimental.

## 5. Final conformance

- [x] 5.1 Enforce ADR-0001 for source and built-package imports, and include all activated packages in the TypeScript solution. Requirement: the foundation has one reproducible quality gate. Standards: 01-source-code.md, 11-ci-and-release.md. Unit: workspace configuration. Verification: three rejected dependency-negative probes and `tsc -b --dry --verbose`.
- [x] 5.2 Reconcile current architecture, ADR-0008, change artifacts, comments, and implementation status. Requirements: all admitted behavior. Standards: 05-documentation.md and the selected matrix. Units: repository documentation and source. Verification: self-containment/temporary-file scans, diff review, `pnpm validate`, and the real-database full suite. Implementation: all tasks completed, 525 tests passing, all gates passing (lint, format, boundaries, openspec), coverage at 87.96%.
